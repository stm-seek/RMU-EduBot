# --- secrets / env ---
.env
.env.*
!.env.example
*.key
*.pem

# --- scraped personal data (PDPA) ---
kb/data/students/
*student_data*.json
*_private.json

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# --- scraper cache / db ---
kb/data/raw/
kb/data/*.db
kb/data/*.db-journal

# --- editor / os ---
.vscode/
.idea/
Thumbs.db
desktop.ini

# สำรอง webhook URL เดิมของ LINE ก่อนเปลี่ยนไปใช้ tunnel
.webhook-endpoint.bak
.server.*.log
.cf.*.log
# log ของ uvicorn/cloudflared ที่ Start-Process เขียนไว้ (ชื่อไม่มีจุดนำหน้า)
server.out.log
server.err.log
server.log
server_err.log
tunnel.log
tunnel_err.log
.claude/
.funnel.log
.tslog.txt
.cf.log
