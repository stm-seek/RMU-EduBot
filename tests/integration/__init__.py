"""
เทสที่ต้องมี **Postgres จริง** (ไม่ใช่ mock)

มี ``__init__.py`` ให้เหมือน ``tests/`` เพื่อกันชื่อโมดูลชนกับเทสชั้นบน
(เช่น ``test_repository.py`` ที่มีอยู่แล้ว) ตอน pytest import แบบ prepend
"""
