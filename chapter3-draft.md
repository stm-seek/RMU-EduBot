# บทที่ 3
# วิธีดำเนินงานวิจัย

บทนี้อธิบายวิธีการดำเนินงานวิจัยและพัฒนาแชทบอทให้คำปรึกษาด้านการเรียนบนแพลตฟอร์มไลน์ (LINE) โดยแบ่งเนื้อหาออกเป็น 5 ส่วน ได้แก่ (1) ขั้นตอนการดำเนินงานวิจัย (2) เครื่องมือและเทคโนโลยีที่ใช้ในการพัฒนา (3) การออกแบบสถาปัตยกรรมระบบ (4) การออกแบบฐานข้อมูลและการรวบรวมฐานความรู้ และ (5) แนวทางการทดสอบและประเมินผลระบบ

---

## 3.1 ขั้นตอนการดำเนินงานวิจัย

การวิจัยครั้งนี้เป็นการวิจัยเชิงพัฒนา (Developmental Research) โดยผู้วิจัยพัฒนาระบบต้นแบบและปรับปรุงเป็นรอบซ้ำ (Iterative Prototype) ตามขั้นตอนดังนี้

**ขั้นที่ 1 ศึกษาความต้องการและขอบเขตของระบบ**
ผู้วิจัยศึกษาปัญหาการให้คำปรึกษาด้านการเรียนของนักศึกษา สำรวจระบบสารสนเทศที่มีอยู่ของมหาวิทยาลัย และกำหนดขอบเขตของระบบต้นแบบให้ครอบคลุมหลักสูตรเป้าหมาย คือ หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาดิจิทัลมีเดียอินโนเวชัน (รหัส 643170151) ซึ่งมี 15 หมวดวิชา 68 รายวิชา รวม 120 หน่วยกิต

**ขั้นที่ 2 รวบรวมและจัดทำฐานความรู้**
ผู้วิจัยพัฒนาโปรแกรมดึงข้อมูลอัตโนมัติ (web scraper) เพื่อดึงข้อมูลสาธารณะจากระบบทะเบียนของมหาวิทยาลัย (regis.rmu.ac.th) โดยไม่มีการเข้าสู่ระบบและไม่ใช้ข้อมูลส่วนบุคคลของนักศึกษา แล้วจัดเก็บเป็นฐานข้อมูลเชิงสัมพันธ์สำหรับให้ระบบสืบค้น

**ขั้นที่ 3 ออกแบบสถาปัตยกรรมระบบ**
ออกแบบสถาปัตยกรรมแบบ 3 ชั้น (รายละเอียดในหัวข้อ 3.4) โดยยึดหลักการว่า การคำนวณที่ต้องให้ผลถูกต้องแน่นอน เช่น การตรวจสอบวิชาบังคับก่อน หน่วยกิต และการตรวจตารางเรียนชนกัน ต้องเป็นโค้ดที่กำหนดพฤติกรรมได้แน่นอน (deterministic code) ไม่มอบหมายให้โมเดลภาษาขนาดใหญ่เป็นผู้คำนวณ ส่วนโมเดลภาษาทำหน้าที่เพียงเรียบเรียงคำอธิบายจากผลลัพธ์ที่คำนวณแล้วเท่านั้น

**ขั้นที่ 4 พัฒนาระบบ**
พัฒนาระบบส่วนหลัง (backend) เพื่อรับส่งข้อความกับแพลตฟอร์มไลน์ พัฒนาส่วนเชื่อมต่อฐานข้อมูล และเชื่อมต่อโมเดลภาษาขนาดใหญ่สำหรับการตอบคำถามทั่วไป โดยพัฒนาควบคู่กับการเขียนชุดทดสอบอัตโนมัติตลอดกระบวนการ

**ขั้นที่ 5 ทดสอบระบบ**
ทดสอบด้วยชุดทดสอบอัตโนมัติ (unit test) ที่ไม่ต้องพึ่งพาอินเทอร์เน็ตหรือฐานข้อมูลจริง และทดสอบระบบเชื่อมต่อ (integration test) กับฐานข้อมูลจริง รวมถึงทดสอบการใช้งานจริงผ่านแอปพลิเคชันไลน์

**ขั้นที่ 6 ประเมินผลและสรุปผล**
ระบบบันทึกข้อมูลการสนทนาทุกครั้งลงในตารางบันทึก (chat_logs) เพื่อใช้วิเคราะห์อัตราการตอบไม่ได้ (fallback rate) สัดส่วนภาระการตอบของแต่ละชั้น และต้นทุนการใช้งานโมเดลภาษา ดังรายละเอียดในหัวข้อ 3.9

---

## 3.2 เครื่องมือและเทคโนโลยีที่ใช้ในการพัฒนา

### 3.2.1 ฮาร์ดแวร์

| รายการ | รายละเอียด |
|---|---|
| เครื่องคอมพิวเตอร์สำหรับพัฒนา | ระบบปฏิบัติการ Windows, หน่วยประมวลผลกลาง …(ระบุ)… , หน่วยความจำ …(ระบุ)… |
| เครื่องแม่ข่ายฐานข้อมูล | รันภายในเครื่องพัฒนาผ่าน Docker (ระหว่างพัฒนา) / …(ระบุเมื่อ deploy จริง)… |

> *(หมายเหตุ: ให้นักศึกษาเติมสเปกเครื่องจริงของตนเอง)*

### 3.2.2 ซอฟต์แวร์และเทคโนโลยีหลัก

ซอฟต์แวร์ทุกตัวถูกล็อกเวอร์ชัน (version pinning) ไว้ในไฟล์ `requirements.txt` และ `pyproject.toml` เพื่อให้ผลการพัฒนาทำซ้ำได้ (reproducible) โดยชุดเวอร์ชันนี้ผ่านการยืนยันการทำงานจริงเมื่อวันที่ 19 สิงหาคม 2569

**(1) ภาษาไพทอน (Python) เวอร์ชัน 3.13** — ภาษาหลักที่ใช้พัฒนาทั้งระบบ ได้แก่ ส่วน backend, โปรแกรมดึงข้อมูล และชุดทดสอบอัตโนมัติ

**(2) FastAPI เวอร์ชัน 0.115.0** — เฟรมเวิร์กสำหรับสร้างเว็บแอปพลิเคชันแบบ asynchronous เลือกใช้เพราะ reply token ของแพลตฟอร์มไลน์มีอายุสั้นมาก ระบบจึงต้องตอบกลับ HTTP 200 ทันทีแล้วค่อยประมวลผลภายหลัง ซึ่ง FastAPI มีกลไก `BackgroundTasks` รองรับรูปแบบการทำงานดังกล่าวโดยตรง (FastAPI, ม.ป.ป.)

**(3) Uvicorn เวอร์ชัน 0.42.0** — เว็บเซิร์ฟเวอร์แบบ ASGI สำหรับรัน FastAPI

**(4) Pydantic และ pydantic-settings เวอร์ชัน 2.12.5 และ 2.15.0** — ตรวจสอบความถูกต้องของข้อมูลขาเข้าและจัดการค่าการตั้งค่าระบบจากไฟล์ .env พร้อมกลไก fail-fast ที่ตรวจสอบค่าที่จำเป็นทันทีตอนเริ่มต้นระบบ

**(5) httpx เวอร์ชัน 0.28.1** — ไลบรารี HTTP client แบบ asynchronous ใช้เรียก LINE API และ API ของโมเดลภาษา นอกจากนี้ยังใช้เป็น backend ของ TestClient ในการทดสอบด้วย

**(6) PostgreSQL เวอร์ชัน 17** — ระบบจัดการฐานข้อมูลเชิงสัมพันธ์ เลือกใช้เพราะเป็นระบบระดับ production ที่รองรับข้อมูลแบบ JSONB และ array (PostgreSQL Global Development Group, ม.ป.ป.)

**(7) pgvector** — ส่วนขยาย (extension) ของ PostgreSQL สำหรับจัดเก็บและสืบค้นเวกเตอร์ (vector similarity search) เลือกใช้แทนระบบจัดการฐานข้อมูลเวกเตอร์แยกต่างหาก (เช่น Chroma หรือ Qdrant) เพื่อลดจำนวนบริการที่ต้องดูแลและลดต้นทุนการดำเนินงาน

**(8) pg_trgm** — ส่วนขยายของ PostgreSQL สำหรับค้นหาข้อความแบบคลุมเครือ (fuzzy/trigram search) จำเป็นเพราะภาษาไทยไม่มีตัวตัดคำใน PostgreSQL จึงไม่สามารถใช้ full-text search กับภาษาไทยโดยตรงได้

**(9) psycopg เวอร์ชัน 3.3.4 และ psycopg-pool เวอร์ชัน 3.3.1** — ไดรเวอร์เชื่อมต่อฐานข้อมูลแบบ asynchronous พร้อมการจัดการ connection pool (ใช้ค่าต่ำ คือ 1–4 connection เพื่อให้ทำงานได้แม้บนบริการฐานข้อมูลแบบ free tier ที่จำกัดจำนวนการเชื่อมต่อ)

**(10) Docker และ Docker Compose** — ใช้รัน PostgreSQL พร้อม pgvector ใน container ระหว่างพัฒนา (image `pgvector/pgvector:pg17`) โดยผูกพอร์ตเฉพาะ 127.0.0.1 ไม่เปิดฐานข้อมูลออกสู่เครือข่าย และรัน migration อัตโนมัติเมื่อสร้าง volume ครั้งแรก

### 3.2.3 เครื่องมือสำหรับดึงข้อมูล (web scraper)

| ไลบรารี | เวอร์ชัน | หน้าที่ |
|---|---|---|
| requests | 2.32.5 | ส่ง HTTP request ดึงหน้าเว็บ |
| urllib3 | 2.6.3 | จัดการการเชื่อมต่อ SSL (เว็บมหาวิทยาลัยมีใบรับรองที่ไม่สมบูรณ์) |
| BeautifulSoup4 | 4.15.0 | แยกโครงสร้างเอกสาร HTML |
| lxml | 6.1.1 | ตัวแยก (parser) HTML — จำเป็นต้องใช้แทน html.parser ในตัวไพทอน เพราะ html.parser ตัดแท็ก `<td>` ทิ้งทำให้ดึงข้อมูลตารางไม่ได้ |
| sqlglot | 30.17.0 | ตรวจสอบความถูกต้องของไวยากรณ์ SQL โดยไม่ต้องต่อฐานข้อมูลจริง |

การดึงข้อมูลกำหนดให้หน่วงเวลาระหว่างคำขออย่างน้อย 1 วินาที เพื่อไม่สร้างภาระต่อเซิร์ฟเวอร์ของมหาวิทยาลัย

### 3.2.4 โมเดลภาษาขนาดใหญ่และเวกเตอร์

ระบบเรียกใช้โมเดลภาษาผ่านอินเทอร์เฟซแบบ OpenAI-compatible ซึ่งทำให้สามารถสลับผู้ให้บริการได้ด้วยการเปลี่ยนค่า `base_url` และ `api_key` เพียงสองค่า โดยไม่แก้ไขโค้ด ผู้ให้บริการที่ทดสอบแล้วว่าใช้งานได้ ได้แก่ Google Gemini, OpenAI, OpenRouter และ Ollama

| ส่วนประกอบ | โมเดลที่ใช้ | เหตุผล |
|---|---|---|
| การสนทนา (AI Chat) | gemini-3.5-flash-lite | จากการทดสอบความเร็ว พบว่าเร็วกว่า gemini flash รุ่นเต็มประมาณ 20 เท่า (ประมาณ 1 วินาที เทียบกับ 20.5 วินาที) โดยคุณภาพคำตอบในงานนี้ไม่แตกต่างกัน เนื่องจากโมเดลทำหน้าที่เพียงเรียบเรียงข้อมูล ไม่ได้คำนวณ |
| การสร้างเวกเตอร์ (embedding) | gemini-embedding-2 | สร้างเวกเตอร์ขนาด 768 มิติ (ยืนยันแล้วว่าคืนเวกเตอร์ที่ถูก normalize แล้ว) |

การออกแบบนี้ตั้งใจไม่ใช้ SDK เฉพาะของผู้ให้บริการรายใดรายหนึ่ง เพื่อไม่ให้ระบบผูกขาดกับผู้ให้บริการเดียว

### 3.2.5 แพลตฟอร์มไลน์

| เทคโนโลยี | หน้าที่ |
|---|---|
| LINE Messaging API | รับ–ส่งข้อความกับผู้ใช้ผ่าน webhook และ reply/push message (LINE Corporation, ม.ป.ป.) |
| LIFF (LINE Front-end Framework) | หน้าเว็บภายในแอปไลน์ สำหรับให้นักศึกษากรอกข้อมูลด้วยตนเอง (ออกแบบไว้สำหรับให้ผู้ใช้แจ้งรายวิชาที่ผ่านแล้ว) |
| Quick Reply | ปุ่มเลือกหัวข้อแบบด่วน แสดงท้ายข้อความ |
| HTTPS tunnel (cloudflared) | เปิดช่องทาง HTTPS จากอินเทอร์เน็ตมายังเซิร์ฟเวอร์ในเครื่องพัฒนา เนื่องจาก LINE กำหนดให้ webhook URL ต้องเป็น HTTPS |

### 3.2.6 เครื่องมือสำหรับการทดสอบ

| เครื่องมือ | เวอร์ชัน | หน้าที่ |
|---|---|---|
| pytest | 9.1.1 | เฟรมเวิร์กการทดสอบหลัก |
| pytest-asyncio | 1.4.0 | รองรับ asynchronous test |
| httpx.MockTransport | (มากับ httpx) | จำลองการตอบกลับของ LINE API และ LLM API โดยไม่แตะเครือข่ายจริง |
| fake connection | (พัฒนาเอง) | จำลองฐานข้อมูล PostgreSQL โดยไม่ต้องติดตั้ง Postgres |

### 3.2.7 เทคโนโลยีที่ตั้งใจไม่ใช้

เพื่อให้ระบบเรียบง่าย ตรวจสอบและแก้ไขจุดผิดพลาดได้ง่าย ผู้วิจัยตัดสินใจไม่ใช้เทคโนโลยีต่อไปนี้

| เทคโนโลยีที่ไม่ใช้ | เหตุผล |
|---|---|
| LangChain | มี abstraction มากเกินความจำเป็นสำหรับขอบเขตงานนี้ การเขียนส่วน RAG เองทำให้เข้าใจและแก้จุดผิดพลาดได้ง่ายกว่า |
| Celery | กลไก BackgroundTasks ของ FastAPI เพียงพอสำหรับปริมาณการใช้งานระดับนี้ |
| ฐานข้อมูลเวกเตอร์แยก (เช่น Chroma, Qdrant) | pgvector ใน PostgreSQL ตัวเดียวเพียงพอ ลดจำนวนบริการที่ต้องดูแล |
| SDK เฉพาะของผู้ให้บริการ LLM | เขียนการเรียก API ตรงด้วย httpx ทำให้สลับผู้ให้บริการได้จริง |

---

## 3.3 การออกแบบสถาปัตยกรรมระบบ

### 3.3.1 ภาพรวมสถาปัตยกรรม

ระบบประกอบด้วย 4 ส่วนหลัก คือ (1) แพลตฟอร์มไลน์ซึ่งเป็นช่องทางสนทนา (2) เว็บแอปพลิเคชัน FastAPI ซึ่งทำหน้าที่รับ webhook ตรวจสอบความถูกต้องของข้อความ และตัดสินใจเลือกคำตอบ (3) ฐานข้อมูล PostgreSQL ซึ่งเก็บฐานความรู้และประวัติการสนทนา และ (4) บริการโมเดลภาษา (Google Gemini) สำหรับสร้างคำตอบในชั้นที่ 3

```
ผู้ใช้ (แอป LINE)
      │  ข้อความ / กดปุ่ม
      ▼
LINE Platform ──── webhook (HTTPS) ────▶ FastAPI (/webhook)
                                            │
                       1. ตรวจลายเซ็นดิจิทัล (HMAC-SHA256)
                       2. ตอบ HTTP 200 ทันที
                       3. ประมวลผลในเบื้องหลัง (BackgroundTask)
                                            │
                        ┌───────────────────┼───────────────────┐
                        ▼                   ▼                   ▼
                  ชั้นที่ 1            ชั้นที่ 2           ชั้นที่ 3
                  ตอบจากฐานข้อมูล     FAQ matching        AI Chat (LLM)
                  ตรง ๆ               (ออกแบบไว้)         Gemini
                  ฟรี / แม่นยำ 100%   ยังไม่บรรจุเนื้อหา   ตอบคำแนะนำทั่วไป
                        │                   │                   │
                        └───────────────────┼───────────────────┘
                                            ▼
                              ไม่มั่นใจ / ไม่มีข้อมูล
                              → ตอบตามตรงว่าไม่มีข้อมูล (ไม่เดา)
                                            │
                                            ▼
                        ส่งคำตอบกลับผ่าน Reply / Push Message
                        และบันทึกการสนทนาลง chat_logs
```

### 3.3.2 กระบวนการรับ webhook

reply token ที่ไลน์ส่งมาให้มีอายุจำกัดมาก หากเซิร์ฟเวอร์ใช้เวลาประมวลผลนาน (เช่น รอโมเดลภาษา 3–10 วินาที) token อาจหมดอายุก่อนตอบกลับได้ ระบบจึงทำงานตามลำดับดังนี้

1. ตรวจสอบลายเซ็นดิจิทัลใน header `X-Line-Signature` ซึ่งไลน์คำนวณด้วย HMAC-SHA256 จากเนื้อหาข้อความโดยใช้ channel secret เป็นคีย์ การเปรียบเทียบใช้ฟังก์ชัน `hmac.compare_digest` เพื่อป้องกันการโจมตีแบบจับเวลา (timing attack) หากไม่ผ่านการตรวจสอบ ระบบปฏิเสธด้วย HTTP 403
2. ตอบกลับ HTTP 200 ทันที เพื่อป้องกันไลน์ส่งคำขอซ้ำ (retry) เพราะเข้าใจว่าเซิร์ฟเวอร์ล่ม
3. นำเหตุการณ์เข้าคิวประมวลผลเบื้องหลัง (BackgroundTask) ซึ่งคำนวณคำตอบ แล้วตอบผู้ใช้ด้วย reply token หากยังทัน หรือส่งเป็น push message หาก token หมดอายุแล้ว
4. หลังจากส่งคำตอบสำเร็จแล้ว จึงบันทึกการสนทนาลงตาราง `chat_logs` — หากการบันทึกล้มเหลว จะไม่กระทบต่อคำตอบที่ผู้ใช้ได้รับ

### 3.3.3 สถาปัตยกรรมตอบกลับแบบ 3 ชั้น

การตัดสินใจเลือกคำตอบเป็นแบบลำดับชั้น โดยแต่ละชั้นมีต้นทุนและความแม่นยำต่างกัน

**ชั้นที่ 1 — ตอบจากฐานข้อมูลโดยตรง (พัฒนาแล้ว)**
ครอบคลุม: การกดปุ่มเมนู (postback), การพิมพ์รหัสวิชา 7 หลัก, และการค้นหาคำที่พิมพ์ด้วย trigram similarity (pg_trgm) เช่น ชื่อเอกสาร ชื่ออาจารย์ ชั้นนี้ไม่ต้องเรียกโมเดลภาษา จึงไม่มีต้นทุนและความถูกต้อง 100% เพราะตอบจากข้อมูลที่จัดเก็บไว้แล้ว

**ชั้นที่ 2 — FAQ matching (ออกแบบโครงสร้างไว้ ยังไม่บรรจุเนื้อหา)**
จับคู่คำถามกับคำถามที่พบบ่อยซึ่งเจ้าหน้าที่เขียนคำตอบไว้ล่วงหน้า โดยใช้ทั้งคีย์เวิร์ดและความคล้ายคลึงของเวกเตอร์ โครงสร้างตาราง `faqs` พร้อมใช้งาน แต่เนื้อหาต้องเป็นข้อมูลทางการที่มาจากหน่วยงาน จึงยังไม่บรรจุ

**ชั้นที่ 3 — AI Chat ด้วยโมเดลภาษา (พัฒนาแล้ว)**
เมื่อการสืบค้นในชั้นที่ 1 ไม่พบข้อมูล และเงื่อนไขครบถ้วน ระบบส่งคำถามพร้อมประวัติการสนทนาไปยังโมเดลภาษา เพื่อตอบคำถามทั่วไปด้านการเรียนและชีวิตมหาวิทยาลัย โดยมีกลไกควบคุมดังนี้

- **System prompt** กำหนดกฎเข้มงวด: ห้ามแต่งข้อมูลทางการของมหาวิทยาลัย (วัน-กำหนดการ ระเบียบ ค่าธรรมเนียม ขั้นตอนเอกสาร) ห้ามขอข้อมูลส่วนตัว และต้องตอบว่าไม่มีข้อมูลเมื่อไม่แน่ใจ
- **บริบทการสนทนาแยกตามผู้ใช้** — ประวัติไม่ได้เก็บในหน่วยความจำของกระบวนการ แต่ดึงจากตาราง `chat_logs` โดยกรองตามรหัสผู้ใช้ จึงไม่ปนกันระหว่างผู้ใช้ และไม่หายไปเมื่อระบบเริ่มทำงานใหม่
- **การควบคุมขนาดบริบท** — ดึงประวัติไม่เกิน 4 รอบล่าสุด รวมไม่เกิน 2,000 ตัวอักษร และตัดข้อความแต่ละรายการที่ 1,000 ตัวอักษร เพื่อป้องกัน prompt บวม
- **การจัดการความล้มเหลว** — เมื่อโมเดลภาษา timeout, ได้รหัส 429, หรือตอบกลับว่างเปล่า ระบบตอบว่า "ระบบ AI ขัดข้องชั่วคราว" แทนที่จะเงียบหาย

### 3.3.4 API endpoints ของระบบ

| Endpoint | วิธี | หน้าที่ |
|---|---|---|
| `/webhook` | POST | รับเหตุการณ์จากไลน์ ตรวจสอบลายเซ็น และส่งต่อให้ประมวลผลเบื้องหลัง |
| `/health` | GET | รายงานสถานะการตั้งค่าและการเชื่อมต่อฐานข้อมูล (ไม่เปิดเผยค่าลับ) |
| `/api/liff/login` | POST | ตรวจสอบ ID token จาก LIFF กับเซิร์ฟเวอร์ไลน์ แล้วคืนค่ารหัสผู้ใช้แบบ hash |
| `/api/liff/config` | GET | ส่งค่า LIFF ID และรหัสหลักสูตรให้หน้าเว็บ LIFF |

---

## 3.4 การออกแบบฐานข้อมูล

ฐานข้อมูลออกแบบเป็น 20 ตาราง 21 ดัชนี แบ่งเป็น 4 กลุ่ม ดังนี้

**กลุ่มที่ 1 ฐานความรู้ทางวิชาการ** (ได้จากการดึงข้อมูล)

| ตาราง | เก็บข้อมูล |
|---|---|
| `programs` | โครงสร้างหลักสูตร (จำนวนหน่วยกิตรวม) |
| `categories` | หมวดวิชาในหลักสูตร |
| `courses` | รายวิชา + คำอธิบายรายวิชา + รูปแบบการเปิดสอน |
| `program_courses` | ความสัมพันธ์รายวิชา–หลักสูตร |
| `offerings`, `offering_slots` | การเปิดสอนแต่ละเทอม + คาบเรียน |
| `offering_patterns` | สรุปว่าวิชาใดเปิดเทอมใด (คำนวณจากข้อมูลย้อนหลัง 4 เทอม) |
| `curriculum_rules`, `prerequisites` | เกณฑ์การจบและวิชาบังคับก่อน (โครงสร้างพร้อม ยังไม่มีข้อมูลจากระบบทะเบียน) |
| `documents` | เอกสาร/แบบฟอร์มคำร้อง พร้อมลิงก์ |
| `instructors`, `instructor_affiliations` | ข้อมูลอาจารย์และสังกัด |

**กลุ่มที่ 2 ข้อมูลผู้ใช้** (ออกแบบตามหลัก data minimization)

| ตาราง | เก็บข้อมูล |
|---|---|
| `app_users` | รหัสผู้ใช้แบบ hash เท่านั้น — ไม่เก็บ LINE user ID ดิบ ไม่เก็บชื่อ |
| `user_completed_courses` | รายวิชาที่นักศึกษาแจ้งว่าผ่านแล้ว |
| `liff_sessions` | เซสชันการใช้งาน LIFF |

**กลุ่มที่ 3 การสนทนาและ AI**

| ตาราง | เก็บข้อมูล |
|---|---|
| `chat_logs` | ประวัติการสนทนาทุกครั้ง: ข้อความถาม–ตอบ, ชั้นที่ตอบ, ความมั่นใจ, โมเดลที่ใช้, จำนวน token, เวลาที่ใช้ |
| `ai_sessions` | เซสชันโหมดปรึกษา AI (เริ่ม/จบ/จำนวนรอบ) |
| `faqs`, `rag_chunks` | โครงสร้างสำหรับ FAQ และ RAG (รอเนื้อหาทางการ) โดย `rag_chunks` มีคอลัมน์ vector(768) พร้อมดัชนี HNSW |

**กลุ่มที่ 4 การดูแลระบบ**

| ตาราง | เก็บข้อมูล |
|---|---|
| `scrape_runs` | บันทึกผลการดึงข้อมูลแต่ละครั้ง |

ดัชนีพิเศษ ได้แก่ ดัชนี GIN trigram (pg_trgm) สำหรับค้นหาภาษาไทยบนตาราง `courses`, `documents`, `instructors`, `faqs` และดัชนี HNSW สำหรับค้นหาเวกเตอร์บนตาราง `rag_chunks`

ข้อมูลนำเข้า (seed) ทั้งหมด 1,065 แถว สร้างจากไฟล์ SQL ที่ generate โดยสคริปต์ ไม่แก้ไขด้วยมือ และการ INSERT เป็นแบบ idempotent ผ่านเงื่อนไข ON CONFLICT

---

## 3.5 การรวบรวมและจัดทำฐานความรู้

ผู้วิจัยพัฒนาโปรแกรมดึงข้อมูล 5 ตัว ทำงานกับข้อมูลสาธารณะของระบบทะเบียนมหาวิทยาลัย โดย **ไม่ต้องเข้าสู่ระบบ** จากการวิเคราะห์พบว่าข้อมูลที่จำเป็นต่อการให้คำปรึกษาประมาณร้อยละ 90 เป็นข้อมูลสาธารณะ มีเพียง "รายวิชาที่นักศึกษาผ่านแล้ว" เท่านั้นที่ต้องขอจากตัวนักศึกษาเอง

| โปรแกรม | ข้อมูลที่ได้ |
|---|---|
| `scrape_programs` | โครงสร้างหลักสูตร: 15 หมวด, 68 วิชา, 120 หน่วยกิต |
| `scrape_courses` | คำอธิบายรายวิชา 68/68 วิชา |
| `scrape_offerings` | ตารางสอนย้อนหลัง 4 เทอม (2567/1 – 2568/2): 337 หมู่เรียน, 292 คาบ → สรุปรูปแบบการเปิดสอน (เปิดทั้ง 2 เทอม 25 วิชา / เฉพาะเทอม 1 จำนวน 12 วิชา / เฉพาะเทอม 2 จำนวน 8 วิชา) |
| `scrape_documents` | เอกสาร/แบบฟอร์ม 32 รายการ 11 หมวด (ตรวจสอบแล้วว่าลิงก์เข้าถึงได้ 31 รายการ) |
| `scrape_instructors` | อาจารย์ 28 คน 33 สังกัด พร้อมตรวจสอบกับรายชื่อผู้สอนจริงในตารางสอน |

ข้อค้นพบที่สำคัญคือ ระบบทะเบียนไม่เผยแพร่ข้อมูลวิชาบังคับก่อน (prerequisite) และแผนการเรียนรายปี/เทอม ผู้วิจัยจึงใช้ **รูปแบบการเปิดสอนจริง** (`offering_patterns`) ซึ่งสรุปจากตารางสอนย้อนหลัง เป็นข้อมูลทดแทน ซึ่งสะท้อนความจริงมากกว่าแผนในเอกสารหลักสูตร

---

## 3.6 การออกแบบด้านความปลอดภัยและข้อมูลส่วนบุคคล

ระบบออกแบบให้สอดคล้องกับพระราชบัญญัติคุ้มครองข้อมูลส่วนบุคคล พ.ศ. 2562 (พ.ร.บ.คุ้มครองข้อมูลส่วนบุคคล, 2562) โดยใช้แนวทาง **แผน B — ไม่ใช้รหัสผ่านนักศึกษา** กล่าวคือ ไม่ขอรหัสผ่านระบบทะเบียนจากนักศึกษา เนื่องจากข้อมูลส่วนใหญ่ที่จำเป็นเป็นข้อมูลสาธารณะ และให้นักศึกษาแจ้งรายวิชาที่ผ่านแล้วด้วยตนเองผ่าน LIFF แทน

มาตรการที่นำมาใช้ ได้แก่

1. **การตรวจสอบแหล่งที่มาของข้อความ** — ทุก webhook ต้องมีลายเซ็นดิจิทัล HMAC-SHA256 ที่ถูกต้อง ไม่ยอมรับคำขอที่ไม่มีลายเซ็น เพื่อป้องกันผู้ไม่ประสงค์ดียิงข้อความปลอมเข้าระบบ
2. **การลดทอนข้อมูล (data minimization)** — ไม่เก็บ LINE user ID ดิบ แต่เก็บเป็นค่า hash SHA-256 ผสม pepper (ความยาวอย่างน้อย 32 ตัวอักษร) ทำให้ไม่สามารถย้อนกลับเป็นตัวตนผู้ใช้ได้ แต่ยังคงระบุผู้ใช้คนเดิมได้ตลอดการสนทนา
3. **ข้อมูลที่ไม่เก็บ** — ไม่เก็บชื่อ–นามสกุล รหัสนักศึกษา เกรด เลขบัตรประชาชน หรือรหัสผ่านใด ๆ
4. **การยืนยันตัวตนฝั่งเซิร์ฟเวอร์** — สำหรับ LIFF ระบบไม่เชื่อถือ userId ที่ฝั่ง client ส่งมา (ซึ่งปลอมแปลงได้) แต่ตรวจสอบ ID token กับ endpoint ของไลน์ (`/oauth2/v2.1/verify`) และใช้ค่า `sub` จากผลการตรวจสอบเท่านั้น ตามที่เอกสารของไลน์เองเตือนไว้
5. **การแยกค่าความลับ** — ค่าลับทั้งหมด (channel secret, API key, pepper) เก็บในไฟล์ .env และบังคับว่าสภาพแวดล้อม production ต้องตั้งค่าครบก่อนเริ่มทำงาน (fail-fast)
6. **การจำกัดการเข้าถึงฐานข้อมูล** — ระหว่างพัฒนา ฐานข้อมูลผูกพอร์ตเฉพาะ localhost ไม่เปิดออกเครือข่าย

---

## 3.7 การทดสอบระบบ

การทดสอบแบ่งเป็น 3 ระดับ

**ระดับที่ 1 Unit test** — จำนวน 296 กรณีทดสอบ ครอบคลุมชั้น router, การตรวจลายเซ็น, การ hash ผู้ใช้, การต่อข้อความ, การจัดการบริบท AI และการ retry ของ LLM จุดเด่นคือ **ไม่ต้องเชื่อมต่ออินเทอร์เน็ตและไม่ต้องติดตั้ง PostgreSQL** โดยจำลองการเรียก LINE API และ LLM API ด้วย `httpx.MockTransport` และจำลองฐานข้อมูลด้วย connection ปลอม ทำให้รันชุดทดสอบซ้ำได้เสมอและเร็ว

**ระดับที่ 2 doctest และตรวจสอบ SQL** — ตัวอย่างใน docstring 17 จุดถูกทดสอบเป็นโค้ดจริง และไฟล์ SQL ทุกไฟล์ถูกตรวจไวยากรณ์ด้วย sqlglot

**ระดับที่ 3 Integration test** — รันกับฐานข้อมูล PostgreSQL 17 + pgvector จริงผ่าน Docker โดย opt-in ผ่านตัวแปรสิ่งแวดล้อม (ไม่รันในสภาวะปกติ) เพื่อยืนยันว่า schema, seed data, pool และการ query ทำงานถูกต้องบนฐานข้อมูลจริง

นอกจากนี้ผู้วิจัยยังทดสอบระบบกับบัญชีไลน์จริงผ่าน HTTPS tunnel เพื่อยืนยันการรับ–ส่งข้อความในสภาวะใช้งานจริง

---

## 3.8 การประเมินผลการใช้งาน

เนื่องจากทุกการสนทนาถูกบันทึกลงตาราง `chat_logs` พร้อมข้อมูลประกอบครบถ้วน ระบบจึงสามารถวัดผลเชิงปริมาณได้โดยตรงจากข้อมูลการใช้งานจริง โดยตัวชี้วัดที่กำหนดไว้ ได้แก่

| ตัวชี้วัด | แหล่งข้อมูล |
|---|---|
| อัตราการตอบไม่ได้ (fallback rate) | คอลัมน์ `answered_by` — นับสัดส่วนที่เป็น `fallback`/`no_data` |
| สัดส่วนภาระของแต่ละชั้น | คอลัมน์ `answered_by` — แยกตาม rich_menu / search / ai_chat |
| ความแม่นยำของการสืบค้น | คอลัมน์ `confidence` (คะแนน trigram similarity) เทียบกับคำตอบที่ถูกต้อง |
| ต้นทุนการใช้โมเดลภาษา | คอลัมน์ `prompt_tokens`, `output_tokens` |
| ความเร็วในการตอบสนอง | คอลัมน์ `latency_ms` |

การประเมินความพึงพอใจ/ประโยชน์ใช้สอย (ถ้ามี) ทำด้วย …*(ระบุวิธีการ เช่น แบบสอบถามกับกลุ่มตัวอย่าง)*…

---

## หมายเหตุถึงนักศึกษา (ลบออกก่อนส่ง)

1. **จุดที่ต้องเติมเอง**: สเปกเครื่อง (3.2.1), วิธีประเมินความพึงพอใจ (3.8), และถ้ามีบทที่อาจารย์กำหนดเพิ่ม เช่น ประชากรและกลุ่มตัวอย่าง หรือสถิติที่ใช้
2. **รายการอ้างอิงที่ควรใส่** (ในบทนี้ผมอ้างแบบนามปีไว้ 4 จุด):
   - FastAPI — https://fastapi.tiangolo.com/
   - PostgreSQL — https://www.postgresql.org/
   - LINE Messaging API docs — https://developers.line.biz/en/docs/messaging-api/
   - พ.ร.บ.คุ้มครองข้อมูลส่วนบุคคล พ.ศ. 2562
   - แนะนำเพิ่ม: เอกสาร pgvector (https://github.com/pgvector/pgvector), Gemini API (https://ai.google.dev/)
3. **ตัวเลขในบทนี้มาจากของจริงทั้งหมด**: 1,065 แถว seed, 20 ตาราง/21 ดัชนี, 296 เทส, 68 วิชา, 32 เอกสาร, 28 อาจารย์, 337 หมู่เรียน — ถ้า re-scrape หรือเพิ่มเทสทีหลัง ต้องอัปเดตตัวเลขเหล่านี้ด้วย
4. **สถานะงานที่ "ยังไม่ได้ทำ"** ในบทนี้เขียนเป็น "ออกแบบไว้/ยังไม่บรรจุเนื้อหา" (เช่น FAQ, RAG, planner, LIFF หน้าเว็บ) — ห้ามแก้เป็น "ทำเสร็จแล้ว" เพราะอาจารย์ตรวจเจอจากตาราง DB ได้ง่าย ๆ ว่า `faqs`/`rag_chunks`/`curriculum_rules` ว่างอยู่
