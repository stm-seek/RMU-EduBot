# ============================================================================
#  Dependency เพิ่มเติมสำหรับ scraper (kb/), ตรวจ SQL (db/) และรันเทส
#
#  ติดตั้ง:  python -m pip install -r requirements-dev.txt
#            (บรรทัด -r ข้างล่างดึง requirements.txt มาให้ด้วย)
#
#  เวอร์ชัน pin ไว้ตรงกับ environment ที่ยืนยันแล้วว่าใช้งานได้จริง
#  (ตรวจเมื่อ 2026-08-19 — Python 3.13.14, เทสผ่าน 244 tests + 17 doctests)
# ============================================================================

-r requirements.txt

# ── scraper + ตรวจ SQL ──────────────────────────────────────────────────────
requests==2.32.5
# import ตรงใน kb/ เพื่อเรียก disable_warnings() (เว็บมหาลัยใบ cert ไม่ผ่าน)
urllib3==2.6.3
beautifulsoup4==4.15.0
# ต้องมี — kb/client.py ตั้ง PARSER = "lxml" เพราะ html.parser ตัด <td> ทิ้ง
lxml==6.1.1
sqlglot==30.17.0

# ── เทส ─────────────────────────────────────────────────────────────────────
pytest==9.1.1
# pytest.ini ตั้ง asyncio_mode = auto ต้องมีปลั๊กอินนี้ ไม่งั้นเทส async ไม่รัน
pytest-asyncio==1.4.0
